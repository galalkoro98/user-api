import { resetBtn } from "./template.js";
// import { resetBtn, container, searchInput } from "./template.js";

// reset button
resetBtn.addEventListener("click", function () {
  // searchInput.value = "";
  // container.innerHTML = "";
  window.location.reload();

  //   console.log("reset");
});

// refresh the page
